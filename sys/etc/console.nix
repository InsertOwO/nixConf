{config, ...}:

{
  console.colors = [
    "${config.col.black1}"
    "${config.col.red1}"
    "${config.col.green1}"
    "${config.col.yellow1}"
    "${config.col.blue1}"
    "${config.col.magenta1}"
    "${config.col.cyan1}"
    "${config.col.white1}"

    "${config.col.black2}"
    "${config.col.red2}"
    "${config.col.green2}"
    "${config.col.yellow2}"
    "${config.col.blue2}"
    "${config.col.magenta2}"
    "${config.col.cyan2}"
    "${config.col.white2}"
  ];
}
